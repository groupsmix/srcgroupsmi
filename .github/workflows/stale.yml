name: Stale

on:
  schedule:
    - cron: "0 0 * * *"          # daily at midnight UTC

# Shared workflow pinned to a full commit SHA — do not replace with @main or
# a mutable tag. See .github/workflows/ci.yml for the bump procedure.

jobs:
  stale:
    uses: groupsmix/shared-workflows/.github/workflows/stale.yml@51fb8660b68f03b581204d16201b425fe67e30eb # pinned to groupsmix/shared-workflows main @ 2026-04
    with:
      days-before-stale: 60
      days-before-close: 14
      exempt-issue-labels: "pinned,security,bug"
      exempt-pr-labels: "pinned,security"
